<%- include("partials/header"); -%>

<h1>Home</h1>

<p><%= para1 %></p>

<% para4.forEach(element => { %>
   <h1><%= element.title %></h1> 
   <p><%= element.body.substr(0, 100)+ " ..." %><a href="/posts/<%= element.title %>">Read More</a></p>
<% }) %>     <!-- forEach == for loop. element is a anonymous function -->

<%- include("partials/footer"); -%>